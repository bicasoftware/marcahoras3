import 'package:flutter/material.dart';

class HorasDetail extends StatelessWidget {
  const HorasDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Lista de horas')),
    );
  }
}
