import 'package:flutter/material.dart';

class HorasListTile extends StatelessWidget {
  const HorasListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
